Best Sugar Daddy Apps.

One-time setup (DO side)

1. In your DigitalOcean App Platform dashboard → your app → Settings → App-Level Settings → Deploy Webhook → create one and copy the URL.
2. In your GitHub repo → Settings → Secrets and variables → Actions → add a secret named DO_DEPLOY_WEBHOOK_URL with that URL.

